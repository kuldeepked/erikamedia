.claude/
.deploy/
.deploy-backup/

# Sensitive: contains bcrypt-hashed admin credentials. Never commit.
.admin.json

# Sensitive: shared secret for the monthly finances cron endpoint.
.cron-token

# Operational data — generated on the live site, not source-controlled.
history.json
finances.json
petty-cash.json

# Live SQLite database (financial data) — never commit.
db/

# Attendance module — secrets + generated data. Never commit.
# (gitignore treats inline '#' as literal, so comments go on their own line.)
.attendance-settings.json
.attendance-accounts.json
.attendance-throttle.json
.attendance-devices.json
attendance.json
